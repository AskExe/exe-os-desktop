import React from "react";

/**
 * Work tab — task management dashboard.
 * Shows tasks, status, reviews from exe-os MCP.
 * TODO: Tom implements full UI via /ui-ux-pro-max
 */
export function WorkView() {
  return (
    <div style={{ color: "#98907d", fontFamily: "var(--font-label)", fontSize: 14 }}>
      WORK DASHBOARD — PENDING IMPLEMENTATION
    </div>
  );
}
