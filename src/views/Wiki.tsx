import React from "react";

/**
 * Wiki tab — exe-wiki integration.
 * Embeds or connects to the AnythingLLM-based knowledge base.
 * TODO: Tom implements full UI via /ui-ux-pro-max
 */
export function WikiView() {
  return (
    <div style={{ color: "#98907d", fontFamily: "var(--font-label)", fontSize: 14 }}>
      COMPANY WIKI — PENDING IMPLEMENTATION
    </div>
  );
}
