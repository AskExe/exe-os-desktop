import React from "react";

/**
 * External tab — external agent connections.
 * Shows connected external agents, their status, capabilities.
 * TODO: Tom implements full UI via /ui-ux-pro-max
 */
export function ExternalView() {
  return (
    <div style={{ color: "#98907d", fontFamily: "var(--font-label)", fontSize: 14 }}>
      EXTERNAL AGENTS — PENDING IMPLEMENTATION
    </div>
  );
}
