import React from "react";

/**
 * Team tab — employee roster, status, memory counts.
 * Both internal employees and external agents.
 * TODO: Tom implements full UI via /ui-ux-pro-max
 */
export function TeamView() {
  return (
    <div style={{ color: "#98907d", fontFamily: "var(--font-label)", fontSize: 14 }}>
      TEAM ROSTER — PENDING IMPLEMENTATION
    </div>
  );
}
